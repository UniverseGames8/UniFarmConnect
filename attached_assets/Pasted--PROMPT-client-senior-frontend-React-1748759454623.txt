⸻

🎯 PROMPT ДЛЯ АГЕНТА (ПОЛНАЯ ПРОВЕРКА client/)
🧠 Вступи в роль senior frontend-инженера по React + Telegram WebApp.

🔧 Твоя задача — провести полную проверку и финализацию клиентского кода UniFarm, расположенного в папке client/.

⸻

📋 ШАГИ ПРОВЕРКИ:
 1. Проверь структуру клиентской части (client/):

client/
├── public/                   ✅ index.html
├── src/
│   ├── assets/              ✅ изображения и иконки
│   ├── components/          ✅ UI-элементы (BalanceCard, MissionItem)
│   ├── layouts/             ✅ MainLayout с TopBar, BottomBar
│   ├── pages/               ✅ Home, Wallet, Farming, Missions, Boost, Referral
│   ├── services/            ✅ user.ts, wallet.ts, missions.ts
│   ├── hooks/               ✅ useTelegram(), useBalance()
│   ├── utils/               ✅ formatNumber, parseTon
│   ├── config/              ✅ BASE_URL, initData
│   ├── styles/              ✅ Tailwind или SCSS
│   └── main.tsx            ✅ точка входа

 2. Убедись в корректной Telegram интеграции:
 • window.Telegram.WebApp.initDataUnsafe используется
 • telegram_id, ref_code, username корректно извлекаются и передаются на backend
 • initData передаётся на каждый API-запрос (если нужно)
 3. API-запросы:
 • Все API-запросы отправляются только через /api/v2/...
 • Не должно быть заглушек и локальных данных
 • Все сервисы подключены к backend, ошибки обрабатываются
 4. Компоненты и страницы:
 • Убедись, что каждая страница подключена и работает: Home, Wallet, Missions, Farming, Boost, Referral
 • Проверь отображение баланса, списка заданий, фарминга, рефералов
 • Компоненты не дублируются, находятся в components/
 5. UX-качество:
 • Интерфейс загружается быстро и без ошибок
 • Поддержка мобильных устройств (Telegram WebApp на телефоне)
 • Стилизация соответствует логике: адаптивность, цвета, иконки
 6. Удаление ненужного:
 • Удали все дублирующие файлы, старые компоненты и пустые хуки
 • Удали тестовые/временные страницы (например, TempPage.tsx, DebugUI.tsx)
 • Проверь что никакие старые пути или компоненты не подключены к main.tsx

⸻

📌 Обязательные условия:
 • НЕ создавай дубликатов
 • НЕ переписывай бизнес-логику
 • НЕ добавляй заглушки
 • НЕ создавай пустые папки и модули

⸻

✅ Результат твоей работы:
 • Полностью рабочая и чистая клиентская часть
 • Все страницы и компоненты подключены и работают
 • Telegram WebApp интеграция настроена и протестирована
 • Отсутствуют лишние, дублирующие или временные файлы

⸻