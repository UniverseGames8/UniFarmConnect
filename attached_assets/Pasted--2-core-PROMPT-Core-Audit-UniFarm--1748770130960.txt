✅ ТЗ №2 — ПРОМТ: Проверка системного ядра проекта (core/)

🎯 PROMPT ДЛЯ АГЕНТА (Core Audit UniFarm)
🧠 Вступи в роль senior backend-инженера и архитектора.

🔧 Твоя задача — провести полную проверку системного ядра проекта, расположенного в папке core/, и убедиться, что всё работает корректно и ничего лишнего не мешает модульной архитектуре.

⸻

📁 Структура core/ должна соответствовать следующему шаблону:

core/
├── config/             ✅ переменные окружения, конфиги, secrets
├── db.ts               ✅ подключение к базе Neon
├── logger.ts           ✅ логирование действий
├── middleware/
│   ├── auth.ts         ✅ middleware авторизации
│   ├── errorHandler.ts ✅ глобальная обработка ошибок
│   └── validate.ts     ✅ опционально: валидация данных


⸻

📋 ШАГИ ПРОВЕРКИ:
 1. Проверь, что в core/config/ нет устаревших или временных конфигов (убери test.env, backup.ts, и пр.)
 2. Удостоверься, что db.ts подключается к актуальной базе Neon (production URL)
 3. Удали все old_db.ts, db-connect-dev.ts, db.ts.bak и т.п.
 4. Проверь middleware:
 • auth.ts корректно проверяет JWT и initData
 • errorHandler.ts глобально перехватывает ошибки
 • Удалены старые middleware-файлы, не подключенные к точке входа
 5. Обнови index.ts и modules/index.ts, если что-то поменял

⸻

📌 Важно:
 • НЕ добавляй новые функции
 • НЕ дублируй существующие middleware
 • НЕ оставляй backup- или temp-файлы

✅ Результат:
Сектор core/ полностью готов к продакшену, нет конфликтов, подключение к БД актуально, middleware обрабатываются корректно.

⸻

✅ ТЗ №3 — ПРОМТ: Аудит и очистка бизнес-модулей (modules/)

🎯 PROMPT ДЛЯ АГЕНТА (Modules Deep Audit)
🧠 Вступи в роль senior backend-разработчика с архитектурным фокусом.

🔧 Твоя задача — пройтись по всей папке modules/, проверить каждый модуль по отдельности на ошибки, дублирующие файлы и отклонения от эталонной структуры.

⸻

📁 Шаблонная структура каждого модуля:

modules/имя/
├── controller.ts
├── service.ts
├── routes.ts
├── model.ts            ✅ если модуль использует таблицу
├── types.ts            ✅ при необходимости
├── logic/              ✅ если есть сложная бизнес-логика


⸻

📋 ШАГИ ПРОВЕРКИ:
 1. Пройди по всем модулям (user, wallet, referral, missions, boost, auth, admin, telegram, dailyBonus, farming)
 2. Убедись, что в каждом модуле:
 • Есть controller.ts, service.ts, routes.ts
 • Если используются таблицы — есть model.ts
 • Если есть расчёты, награды, сложные условия — есть logic/
 • Нет лишних файлов: temp.ts, debug.ts, old_*.ts, controller.bak.ts
 3. Проверь на дубли:
 • Например, walletService.ts и wallet-service.ts не должны существовать одновременно
 • Удали все дубляжи и backup-файлы
 4. Проверь экспорт всех компонентов в modules/index.ts
 5. Если что-то отсутствует — создай только структуру (не пиши заглушки)

⸻

📌 Важно:
 • Не вноси бизнес-логику от себя
 • Не создавай заглушки и фейки
 • Придерживайся единого шаблона без отклонений

✅ Результат:
Модули очищены, каждый имеет свою чёткую структуру, отсутствуют дубли и временные файлы. Система на 100% чиста и готова к деплою.

⸻